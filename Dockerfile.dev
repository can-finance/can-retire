FROM node:24-alpine

WORKDIR /workspace

# Install dependencies first for layer caching
COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# --host 0.0.0.0 via the dev:host script so Vite is reachable from outside the container
CMD ["npm", "run", "dev:host"]
